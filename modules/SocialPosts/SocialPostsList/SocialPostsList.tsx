import React from 'react';
import s from './SocialPostsList.module.scss';

type Props = {}

const SocialPostsList = (props: Props) => {
    return (
        <div className={s.socialPostsList}>

        </div>
    )
}

export default SocialPostsList